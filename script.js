
 let elem = document.getElementById('block')
 let elems = document.getElementById('container')
 
/*elems.addEventListener("click",function(event){
	 elem.style.left = event.clientX + 'px'
	 elem.style.top = event.clientY + 'px'
 })*/
 
 /*elems.addEventListener("click",function(event){
	 elem.style.backgroundColor = `#${Math.floor(Math.random() * 16777215).toString(16)}`;
})*/

/*elems.addEventListener("mousemove",function(event){
	elem.style.left = event.clientX + 'px';
	elem.style.top = event.clientY + 'px';
	
	 
}) */

